<Window x:Class="WpfApp1.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <DockPanel>
            <Button DockPanel.Dock="Bottom" Content="Click me"></Button>
            <TextBox DockPanel.Dock="Bottom" Height="20" Text="{Binding InputPath}"></TextBox>
            <TextBlock DockPanel.Dock = "Bottom" Text="{Binding InputPath}"></TextBlock>
            <DataGrid DockPanel.Dock="Bottom" ItemsSource="{Binding Data}"></DataGrid>
            
        </DockPanel>
    </Grid>
</Window>
